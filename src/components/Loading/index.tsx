'use client'

const Loading: React.FC = () => {
  
  return (
    <div className='fixed top-0 left-0 w-screen h-screen flex justify-center items-center bg-slate-800 z-[9999]'>
      <p>loading...</p>
    </div>
  );
}

export default Loading
